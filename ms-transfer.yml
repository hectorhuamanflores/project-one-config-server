server:
  port: 9082

spring:
  data:
    mongodb:
        uri: mongodb+srv://Francois:GZB0halqtzSXXjj5@cluster0.d4xjx.mongodb.net/Deposit?retryWrites=true&w=majority   
  cloud:
    discovery:
        enabled: true
    gateway:
        discovery:
            locator:
                enabled: true
                lower-case-service-id: true              
eureka:
  client:
     fetch-registry: true
     register-with-eureka: true
     service-url:
        default-zone: http://localhost:8761/eureka