server:
  port: 8097
  
spring:
  data:
    mongodb:
        uri: mongodb+srv://bootcam-proyecto-01:z2MmbUXIH0ZOSUlm@cluster0.lds0o.mongodb.net/BOOTCAMP-DB-CREDIT-PAYMENT?retryWrites=true&w=majority
  cloud:
    discovery:
        enabled: true
    gateway:
        discovery:
            locator:
                enabled: true
                lower-case-service-id: true      
        
eureka:
  instance:
     hostname: localhost
     #hostname: eureka-service-contenedor
     prefer-ip-address: true
  client:
     fetch-registry: true
     register-with-eureka: true
     service-url:
        defaultZone: http://${eureka.instance.hostname}:8761/eureka/  